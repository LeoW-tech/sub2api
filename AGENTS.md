# AGENTS.md

请始终用中文与用户交流。

每次完成代码、脚本、配置或文档改动后，请及时提交本地 git；除非用户明确要求，否则不要自动推送远端。

## 运行与合并规则

1. Linux `/srv/sub2api/primary` 是唯一的合并、构建、全量检查工作区。
2. Tokyo `/srv/sub2api/repo` 只做 `git fetch`、`git merge --ff-only` 和只读核对，不运行全量测试、构建、重启或修改生产配置。
3. 合并前先用提交图和 tag 祖先关系确认版本，不依赖 `HEAD` 文本版号或界面显示版本。
4. 全量检查只在 Linux 串行执行，重任务不要同时启动；记录命令、提交 SHA、并发参数、资源快照和结果。
5. 遇到服务健康异常、内存/磁盘压力明显上升时，先停测试进程，再检查原因，不把重启整机作为常规恢复手段。
6. 生产数据、密钥、runtime 目录、数据库目录、`node_modules` 不得进入 Git 或镜像。
7. 禁止 `force push`、`git reset --hard`、无审查覆盖式合并和其他破坏性操作。
